describe('The Modal Region', function () {
	describe('when a view is shown in it', function () {
		it('should transform the view into a modal', function () {
			expect(0).toBe(1);
		});
	});

	describe('the modal view', function () {
		it('should be instantiated with the right options', function () {
			expect(0).toBe(1);
		});

		describe('when closed', function () {
			it('should empty the region', function () {
				expect(0).toBe(1);
			});
		});

		describe('when the right event is triggered on the view', function () {
			it('should close the modal', function () {
				expect(0).toBe(1);
			});
		});
	});
});
